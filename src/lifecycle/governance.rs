//! Governance — enforce per-type capacity limits

/// Enforce per-type capacity limits
pub fn enforce_capacity_limits(_vault: &mut super::super::Vault) {
    // TODO: Check per-type limits
    // Archive oldest/lowest-confidence when over limit
}
